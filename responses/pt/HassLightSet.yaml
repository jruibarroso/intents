language: pt
responses:
  intents:
    HassLightSet:
      brightness: '{{ slots.name }} ajusta brilho para {{ slots.brightness }}'
      brightness_area: O brilho de {{ slots.area }} ajusta para {{ slots.brightness }}
      color: '{{ slots.name }} ajusta a cor para {{ slots.color }}'
      color_area: A cor de {{ slots.area }} ajusta para {{ slots.color }}
