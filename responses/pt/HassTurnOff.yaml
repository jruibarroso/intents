language: pt
responses:
  intents:
    HassTurnOff:
      default: {{ slots.name }} desligado
      lights_area: Desligadas luzem em {{ slots.area }}
      fans_area: Desligadas ventoinhas em {{ slots.area }}
      cover: {{ slots.name }} fechada
      cover_area: {{ slots.area }} fechada
