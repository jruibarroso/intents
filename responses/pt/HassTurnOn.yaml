language: pt
responses:
  intents:
    HassTurnOn:
      default: Ligado {{ slots.name }}
      lights_area: Ligadas as luzes em {{ slots.area }}
      fans_area: Ligadas as ventoinhas em {{ slots.area }}
      cover: {{ slots.name }} foi aberto
      cover_area: {{ slots.area }} foi aberto
